{
  "name": "personio-webflow-sync",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "sync": "curl -X POST http://localhost:8787/sync"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
